import type { ReactNode } from 'react'

export const metadata = {
  title: '캐릭터 | 와넥',
  description: '와넥에서 다양한 AI 캐릭터를 만나보세요',
}

export default function CharactersLayout({ children }: { children: ReactNode }) {
  return children
}
